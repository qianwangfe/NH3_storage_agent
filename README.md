Hydride NH3 Agent is also designed as an **agentic-ready package**: the same registered skills can be accessed through a command-line interface, web interfaces, an HTTP API, and MCP-compatible agent clients such as Codex or Claude Desktop.

## Scientific workflow

The recommended article-aligned demonstration follows this sequence:

1. Compare physical-state diversity across material families.
2. Trace the LiBH4·xNH3 solid–liquid-like–solid sequence.
3. Analyze conductivity against NH3/BH4 under explicit temperature windows.
4. Compare reported H2 release without connecting non-comparable studies.
5. Separate direct evidence, indirect evidence, and a coordination-shell hypothesis.
6. Design USPEX/DFT/MD/MetaD validation and falsification tests.

## Registered skills

| Skill | Databases | Main output |
|---|---|---|
| `state_complexity` | NH3 Storage | Family-by-state count figure and screened CSV files |
| `state_sequence_lookup` | NH3 Storage | Composition-dependent state table with conditions and DOI |
| `conductivity_loading` | DigBat | DOI-aware conductivity figure and CSV files |
| `h2_release_loading` | DigHyd | Condition-aware H2-release scatter plot and CSV |
| `dual_axis_cross_database` | DigBat + DigHyd | Separate-axis cross-database figure |
| `mechanism_synthesis` | Session evidence | Evidence hierarchy, competing explanations, optional causal map |
| `computational_design` | Session evidence | USPEX/DFT/MD/MetaD workflow and falsification criteria |

## Agentic-ready interfaces

The package exposes the same skill registry through multiple interfaces.

| Interface | Entry point | Purpose |
|---|---|---|
| CLI | `hydride-agent ...` | Reproducible local workflows and artifact generation |
| Chainlit | `chainlit run chainlit_nh3_agent.py -w` | Browser-based conversational interface |
| Streamlit | `streamlit run app_streamlit.py` | Browser-based dashboard interface |
| API | `uvicorn hydride_agent.api:app --reload --port 8000` | Programmatic HTTP access |
| MCP | `hydride_agent/mcp_server.py` | Tool invocation by Codex, Claude Desktop, and other MCP-compatible clients |

In this repository, “agentic-ready” means that deterministic scientific skills are exposed as machine-callable tools with structured inputs, controlled outputs, and explicit evidence boundaries. The LLM does not directly inspect full backend tables; it receives compact reports, figures, and exported CSV files generated by the registered skills.

## Representative questions

```text
Using the NH3 Storage dataset, compare the raw counts of solid, mixed/transition, and liquid records across material families. Highlight borohydrides and return the figure and supporting CSV files.
```

```text
Trace the reported state sequence of LiBH4·xNH3 from x = 1 to 3. List the composition, reported state, conditions, evidence type, confidence, and DOI. Determine whether the literature supports a solid–liquid-like–solid sequence.
```

```text
Using DigBat, plot ionic conductivity against NH3/BH4 ratio for LiBH4 near 305 ± 3 K and Mg(BH4)2 near 323 ± 3 K. Use one representative value per formula and DOI, aggregate multiple DOI in log10 conductivity space, and export the plotting data.
```

```text
Using DigHyd, compare reported H2 release across ammoniated borohydrides with NH3/BH4 > 0. Retain the host borohydride, loading, temperature, conditions, and DOI. Do not connect non-comparable studies.
```

```text
Based on the previous state, conductivity, and H2-release evidence, compare NH3 stoichiometry alone, cation effects, and coordination-shell reconstruction. Separate direct evidence, indirect evidence, and hypotheses, and generate an evidence-to-hypothesis map.
```

```text
Design a USPEX, molecular-dynamics, and metadynamics workflow to test whether coordination-shell switching explains LiBH4·NH3, LiBH4·2NH3, and LiBH4·3NH3. Specify collective variables, descriptors, expected observations, uncertainty checks, and falsification criteria.
```

Additional prompts are provided in [`examples/representative_queries.md`](examples/representative_queries.md).

## Installation

```bash
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
```

For editable installation:

```bash
pip install -e .
```

For MCP-based agent clients, make sure the MCP dependencies are installed:

```bash
pip install mcp requests
```

If the CLI front page uses rich terminal formatting, install:

```bash
pip install rich
```

This public repository uses synthetic demonstration data in `data/demo/`. To use authorized full workbooks locally, place them in the ignored `data/raw/` directory or set `HYDRIDE_AGENT_DATA_DIR`.

To select another data directory:

```bash
export HYDRIDE_AGENT_DATA_DIR=/path/to/workbooks
```

On Windows PowerShell:

```powershell
$env:HYDRIDE_AGENT_DATA_DIR="C:\path\to\workbooks"
```

Copy `.env.example` to `.env` only on your local machine. Never commit API keys.

## Run

### Chainlit

```bash
chainlit run chainlit_nh3_agent.py -w
```

### Streamlit

```bash
streamlit run app_streamlit.py
```

### API

```bash
uvicorn hydride_agent.api:app --reload --port 8000
```

Useful API routes include:

```text
/databases
/skills
/chat
/skills/{skill_name}
```

### CLI

```bash
hydride-agent check-data
hydride-agent list-skills
hydride-agent ask "Trace the reported state sequence of LiBH4·xNH3 from x = 1 to 3."
```

If the enhanced agentic CLI is enabled:

```bash
hydride-agent front
hydride-agent list-skills --format table
hydride-agent run-skill state_complexity
hydride-agent demo
```

## MCP / Codex / Claude Desktop

Hydride NH3 Agent can be exposed to MCP-compatible clients through `hydride_agent/mcp_server.py`.

### Start the API backend

Start the backend before using MCP tools if the MCP server is configured to call the local API:

```bash
uvicorn hydride_agent.api:app --reload --port 8000
```

If your MCP configuration uses another port, set `HYDRIDE_API_URL` accordingly.

### Codex MCP configuration

Add an MCP server entry to the Codex configuration file. Use placeholder paths appropriate for your machine:

```toml
[mcp_servers.hydride]
command = "/path/to/.venv/bin/python"
args = ["/path/to/hydride_nh3_agent/hydride_agent/mcp_server.py"]
env = {
  PYTHONPATH = "/path/to/hydride_nh3_agent",
  HYDRIDE_API_URL = "http://127.0.0.1:8000"
}
```

Windows example format:

```toml
[mcp_servers.hydride]
command = "C:\path\to\.venv\Scripts\python.exe"
args = ["C:\path\to\hydride_nh3_agent\hydride_agent\mcp_server.py"]
env = {
  PYTHONPATH = "C:\path\to\hydride_nh3_agent",
  HYDRIDE_API_URL = "http://127.0.0.1:8000"
}
```

After restarting Codex, ask:

```text
List available MCP tools. Do not inspect source code.
```

Expected Hydride tools include:

```text
mcp__hydride.list_databases
mcp__hydride.list_skills
mcp__hydride.state_complexity
mcp__hydride.state_sequence_lookup
mcp__hydride.run_skill
mcp__hydride.answer_question
```

### Claude Desktop MCP configuration

Claude Desktop can use the same MCP server. Add an entry like this to the Claude Desktop MCP configuration file:

```json
{
  "mcpServers": {
    "hydride": {
      "command": "/path/to/.venv/bin/python",
      "args": [
        "/path/to/hydride_nh3_agent/hydride_agent/mcp_server.py"
      ],
      "env": {
        "PYTHONPATH": "/path/to/hydride_nh3_agent",
        "HYDRIDE_API_URL": "http://127.0.0.1:8000"
      }
    }
  }
}
```

Restart Claude Desktop after editing the configuration.

### Example MCP prompts

```text
Use Hydride MCP list_databases and list_skills. Summarize which databases and skills are available for cross-database hydride/NH3 analysis. Do not inspect source code.
```

```text
Use Hydride MCP answer_question to answer:
What is the reported state sequence for LiBH4·xNH3 with x = 1, 2, and 3?
Do not inspect source code.
```

```text
Use Hydride MCP tools to perform a cross-database state–descriptor analysis.

Compare NH3-containing hydride materials and explain why NH3 stoichiometry alone does not determine solid/liquid behavior.

Include LiBH4·NH3, LiBH4·2NH3, LiBH4·3NH3, and at least 3 additional NH3-containing or ammine borohydride systems if available.

Create:
1. a cross-data evidence table,
2. an ASCII descriptor map with framework connectivity on the x-axis and NH3 coordination/solvation degree on the y-axis,
3. a final scientific conclusion.

Do not inspect source code.
```

## Reproduce the representative outputs

```bash
python scripts/reproduce_all_examples.py
```

The script runs the five main database/mechanism demonstrations in one session and writes reports and artifacts to `outputs/reproduction/`.

For a quick agentic-ready smoke test, use:

```bash
hydride-agent check-data
hydride-agent list-skills
hydride-agent run-skill state_complexity
```

If the enhanced CLI is enabled:

```bash
hydride-agent front
hydride-agent demo
```

## Data and public-release policy

The GitHub package contains **synthetic demonstration workbooks only**. They test code paths and must not be used for scientific conclusions. Full NH3 Storage, DigBat, and DigHyd workbooks are intentionally excluded from the public repository.

Before releasing any full dataset, verify:

- redistribution rights and database licenses;
- whether extracted literature text or figures may be shared;
- whether local paths, logs, emails, author metadata, or API keys are present;
- whether DOI-level records require a separate data citation.

## Testing

```bash
pytest -q
```

## Output boundary

The front ends receive only:

- a public report built from compact evidence;
- generated figures;
- explicit CSV exports.

`AgentResult.data_preview` remains empty, so unrestricted backend tables are not rendered directly.

## Troubleshooting

### CLI output appears in the terminal, not in the browser

This is expected. CLI commands are terminal tools. To use a browser interface, start Chainlit or Streamlit.

### MCP tools do not appear

Check that:

- `hydride_agent/mcp_server.py` exists;
- `mcp` and `requests` are installed;
- the MCP client configuration points to the correct Python executable and project path;
- `PYTHONPATH` points to the project root;
- `HYDRIDE_API_URL` points to the running API backend;
- the MCP client was restarted after editing the configuration.

### A skill reports no records after screening

Some skills use strict scientific filters, such as formula, temperature window, system, and DOI screening. If no records remain, the skill should report that outcome instead of generating unsupported trends.

