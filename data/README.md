# Data layout

- `data/raw/`: optional private/full workbooks. This directory is ignored by Git.
- `data/demo/`: synthetic English-language workbooks for testing and demonstrations.
- `data/schema/`: expected fields and scientific roles.

The synthetic records in `data/demo/` are not literature data and must not be used for scientific conclusions.
